export const setProfiles = (profiles) => ({
    type: 'SET_PROFILES_SUCCESS',
    profiles
});

export const setPosts = (posts) => ({
    type: 'SET_POSTS_SUCCESS',
    posts
});

export const setHashedPosts = (hashedPosts) => ({
    type: 'SET_HASHED_POSTS_SUCCESS',
    hashedPosts
});

export const setComments = (comments) => ({
    type: 'SET_COMMENTS_SUCCESS',
    comments
});

export const setImages = (images) => ({
    type: 'SET_IMAGES_SUCCESS',
    images
});

export const setMyProfile = (profile) => ({
    type: 'SET_MY_PROFILE_SUCCESS',
    profile
});
